<!--
ADR template — copy, do NOT edit this file in place.

1. Generate an 8-hex uuid:        openssl rand -hex 4
2. Copy to:  decisions/adr-pending-<uuid>-<slug>.md
   - <uuid> = the 8 lowercase hex from step 1 (must equal frontmatter `uuid`)
   - <slug> = kebab-case topic, e.g. melvor-clean-room
3. Fill the frontmatter + body. Keep `id: pending` — do NOT hand-number.
4. Delete this comment block and the "## How to use" section.
5. PR it. On merge to main, .github/workflows/adr-assign.yml renames the
   file to adr-NNNN-<slug>.md, sets `id: NNNN`, rewrites in-repo references,
   and opens a follow-up numbering PR. `uuid` stays forever as the stable alias.

Invariants enforced by tools/adr-verify.sh + .github/workflows/adr-pr-check.yml:
  - pending file: name adr-pending-<8hex>-<slug>.md, `id: pending`,
    frontmatter `uuid` == filename uuid
  - never add a hand-numbered adr-NNNN-*.md in a PR (the assign Action only)
  - ids stay contiguous 1..N; uuid unique across numbered + pending
-->
---
id: pending
uuid: xxxxxxxx
title: <one-line decision title, no trailing period>
date: 2026-06-02
status: proposed
---

# ADR-XXXX — <same title as frontmatter>

> uuid `xxxxxxxx` (stable alias; authored as `adr-pending-xxxxxxxx-<slug>`,
> numbered on merge). Output of decision acked by <approver> <date>.

## How to use

Delete this whole section before opening the PR. Reminders:

- `status:` one of `proposed` → `accepted` → `superseded by adr-NNNN` / `deprecated`.
- An ADR records a decision already made — it is not the place to make one.
  Reach the decision with the operator first; cite where in the blockquote above.
- No narrative prose. Tables, bullets, fenced blocks, imperative voice.
- Cite `file:line` or URL for every non-obvious claim.
- Supersede by writing a new ADR that marks this one
  `status: superseded by adr-NNNN`. Never delete an ADR to replace it.

## Decision

<!-- The chosen option, stated as fact. Prefer a table for multi-aspect
     decisions; one tight paragraph or bullet list otherwise. -->

| Aspect | Choice |
|---|---|
|  |  |

## Context

<!-- Forces that constrain the decision: state + source for each. -->

| Constraint | Source |
|---|---|
|  |  |

## Consequences

**Positive**
-

**Negative**
-

**Rejected alternatives**
- **<alternative>** — why not (tie to the actual constraints, not generic
  pros/cons).

## References

- PR: #<NN>
- Related ADRs / design docs: `decisions/adr-NNNN-*.md`, `design/*.md`
